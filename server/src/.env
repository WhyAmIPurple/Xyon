DB_HOST=localhost
DB_USER=xyon_app
DB_PASSWORD=XyonAppPass123!
JWT_SECRET=your_jwt_secret_key
# For password reset emails — set to a Gmail address + App Password.
# Leave blank to use console fallback (dev mode: OTP is printed to server logs).
EMAIL_USER=
EMAIL_PASS=